<script src="{{ CONFIG.ASSETSURL }}js/macy.js"></script>
<div class="news-masonry">
    <div class="height_30"></div>
    <div class="container hidden-md hidden-lg">
        <div class="row">
            {% if articles|length > 0 %}
                <div class="col-sm-12 col-xs-12 mas-m-b">
                    <div class="mas-item mas-big" style="margin-bottom:0px;">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[0]} %}
                    </div>
                </div>
            {% endif %}
            {# Its in an odd order to preserve aspect ratio#}
            {% if articles|length > 2 %}
                <div class="col-xs-{{ articles|length < 4 ? 12 : 6 }} mas-m-b" {{ articles|length < 4 ? '' : 'style="padding-right:2px;"' }}>
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[2]} %}
                    </div>
                </div>
                {% if articles|length > 3 %}
                    <div class="col-xs-6  mas-m-b" style="padding-left:2px;">
                        <div class="mas-item masonry-sm">
                            {% include 'elements/masonryArticle.twig' with {'article':articles[3]} %}
                        </div>
                    </div>
                {% endif %}
            {% endif %}
            {% if articles|length > 1 %}
                <div class="col-sm-12 col-xs-12 mas-m-b">
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[1]} %}
                    </div>
                </div>
            {% endif %}
        </div>
        <div class="row">
            {% if articles|length > 4 %}
                <div class="col-sm-12 col-xs-12 mas-m-b">
                    <div class="mas-item mas-big" style="margin-bottom:0px;">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[4]} %}
                    </div>
                </div>
            {% endif %}
            {# Its in an odd order to preserve aspect ratio#}
            {% if articles|length > 6 %}
                <div class="col-xs-{{ articles|length < 8 ? 12 : 6 }} mas-m-b" {{ articles|length < 8 ? '' : 'style="padding-right:2px;"' }}>
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[6]} %}
                    </div>
                </div>
                {% if articles|length > 7 %}
                    <div class="col-xs-6  mas-m-b" style="padding-left:2px;">
                        <div class="mas-item masonry-sm">
                            {% include 'elements/masonryArticle.twig' with {'article':articles[7]} %}
                        </div>
                    </div>
                {% endif %}
            {% endif %}
            {% if articles|length > 5 %}
                <div class="col-sm-12 col-xs-12 mas-m-b">
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[5]} %}
                    </div>
                </div>
            {% endif %}
            {% if articles|length > 8 %}
                <div class="col-xs-{{ articles|length < 10 ? 12 : 6 }} mas-m-b" {{ articles|length < 10 ? '' : 'style="padding-right:2px;"' }}>
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[8]} %}
                    </div>
                </div>
                {% if articles|length > 9 %}
                    <div class="col-xs-6  mas-m-b" style="padding-left:2px;">
                        <div class="mas-item masonry-sm">
                            {% include 'elements/masonryArticle.twig' with {'article':articles[9]} %}
                        </div>
                    </div>
                {% endif %}
            {% endif %}
            {% if articles|length > 10 %}
                <div class="col-xs-{{ articles|length < 12 ? 12 : 6 }} mas-m-b" {{ articles|length < 12 ? '' : 'style="padding-right:2px;"' }}>
                    <div class="mas-item masonry-sm">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[10]} %}
                    </div>
                </div>
                {% if articles|length > 11 %}
                    <div class="col-xs-6  mas-m-b" style="padding-left:2px;">
                        <div class="mas-item masonry-sm">
                            {% include 'elements/masonryArticle.twig' with {'article':articles[11]} %}
                        </div>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
    <div class="container hidden-sm hidden-xs">
        <div class="row mas-m">
            {% if articles|length > 0 %}
                <div class="col-sm-12 col-md-{{ articles|length < 2 ? "12" : "6" }} mas-p">
                    <div class="mas-item{{ articles|length < 3 ? ' masonry-sm' : ' mas-big' }} mas-m-b">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[0]} %}
                    </div>
                </div>
            {% endif %}
            {% if articles|length > 1 %}
                <div class="col-sm-12 col-md-6 mas-p">
                    <div class="row mas-m">
                        <div class="col-lg-12 mas-p">
                            <div class="mas-item masonry-sm mas-m-b">
                                {% include 'elements/masonryArticle.twig' with {'article':articles[1]} %}
                            </div>
                        </div>
                    </div>
                    {% if articles|length > 2 %}
                        <div class="row mas-m">
                            <div class="col-sm-12 col-md-{{ articles|length < 4 ? 12 : 6 }} mas-p">
                                <div class="mas-item masonry-sm mas-m-b">
                                    {% include 'elements/masonryArticle.twig' with {'article':articles[2]} %}
                                </div>
                            </div>
                            {% if articles|length > 3 %}
                                <div class="col-sm-12 col-md-6 mas-p">
                                    <div class="mas-item masonry-sm mas-m-b">
                                        {% include 'elements/masonryArticle.twig' with {'article':articles[3]} %}
                                    </div>
                                </div>
                            {% endif %}
                        </div>
                    {% endif %}
                </div>
            {% endif %}
        </div>
        <div class="row mas-m">
            {% if articles|length > 5 %}
                <div class="col-sm-12 col-md-6 mas-p">
                    <div class="row mas-m">
                        <div class="col-lg-12 mas-p">
                            <div class="mas-item masonry-sm mas-m-b">
                                {% include 'elements/masonryArticle.twig' with {'article':articles[5]} %}
                            </div>
                        </div>
                    </div>
                    {% if articles|length > 6 %}
                        <div class="row mas-m">
                            <div class="col-sm-12 col-md-{{ articles|length < 8 ? 12 : 6 }} mas-p">
                                <div class="mas-item masonry-sm mas-m-b">
                                    {% include 'elements/masonryArticle.twig' with {'article':articles[6]} %}
                                </div>
                            </div>
                            {% if articles|length > 7 %}
                                <div class="col-sm-12 col-md-6 mas-p">
                                    <div class="mas-item masonry-sm mas-m-b">
                                        {% include 'elements/masonryArticle.twig' with {'article':articles[7]} %}
                                    </div>
                                </div>
                            {% endif %}
                        </div>
                    {% endif %}
                </div>
            {% endif %}
            {% if articles|length > 4 %}
                <div class="col-sm-12 col-md-{{ articles|length < 6 ? "12" : "6" }} mas-p">
                    <div class="mas-item{{ articles|length < 7 ? ' masonry-sm mas-m-b' : ' mas-big' }} mas-m-b">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[4]} %}
                    </div>
                </div>
            {% endif %}
        </div>
        <div class="row mas-m">
            {% if articles|length > 8 %}
                <div class="col-sm-12 col-md-{{ articles|length < 10 ? "12" : "6" }} mas-p">
                    <div class="mas-item{{ articles|length < 11 ? ' masonry-sm' : ' mas-big' }}">
                        {% include 'elements/masonryArticle.twig' with {'article':articles[8]} %}
                    </div>
                </div>
            {% endif %}
            {% if articles|length > 9 %}
                <div class="col-sm-12 col-md-6 mas-p">
                    <div class="row mas-m">
                        <div class="col-lg-12 mas-p">
                            <div class="mas-item masonry-sm mas-m-b">
                                {% include 'elements/masonryArticle.twig' with {'article':articles[9]} %}
                            </div>
                        </div>
                    </div>
                    {% if articles|length > 10 %}
                        <div class="row mas-m">
                            <div class="col-sm-12 col-md-{{ articles|length < 12 ? 12 : 6 }} mas-p">
                                <div class="mas-item masonry-sm">
                                    {% include 'elements/masonryArticle.twig' with {'article':articles[10]} %}
                                </div>
                            </div>
                            {% if articles|length > 11 %}
                                <div class="col-sm-12 col-md-6 mas-p">
                                    <div class="mas-item masonry-sm">
                                        {% include 'elements/masonryArticle.twig' with {'article':articles[11]} %}
                                    </div>
                                </div>
                            {% endif %}
                        </div>
                    {% endif %}
                </div>
            {% endif %}
        </div>
    </div>
</div>