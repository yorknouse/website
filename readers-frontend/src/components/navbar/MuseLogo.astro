---
interface Props {
  baseUrl: string;
}

const { baseUrl } = Astro.props;
---

<a href={baseUrl} class="mx-auto self-center">
  {/* This is needed to stretch the svg */}
  <div class="my-4 h-18 w-63 bg-muse-logo bg-fill"></div>
</a>
